apiVersion: v1
kind: Service
metadata:
  name: hajimari
  namespace: hajimari
  labels:
    app: hajimari
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: hajimari
